apiVersion: v2
name: agenta
description: |
  AI-native task management — like Linear, but with AI agents as first-class citizens.
  Self-host the backend, frontend, and PostgreSQL (pgvector) on a Kubernetes cluster.
type: application
version: 0.1.0
appVersion: "latest"
home: https://agenta.ai
sources:
  - https://github.com/ezeslucky/agenta
keywords:
  - agenta
  - issue-tracker
  - ai-agents
  - self-host
